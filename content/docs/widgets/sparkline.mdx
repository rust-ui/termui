---
title: Sparkline
description: A one-line chart for a short sequence of values.
---

`Sparkline` maps values to compact terminal bar characters.

<RustDemo name="sparkline" src="/demos/interactive.html?demo=sparkline-interactive" title="Sparkline" rows={5} code={`use termui_widgets::sparkline::Sparkline;

let values = [1, 3, 2, 5, 4, 7];
Sparkline::new("Activity", &values).render(frame, area);`} />

## Source

<ComponentSource src="crates/termui-registry/src/widgets/src/sparkline.rs" title="sparkline.rs" language="rs" collapsible={false} />

## Usage

```rust
use termui_widgets::sparkline::Sparkline;

let values = [1, 3, 2, 5, 4, 7];
Sparkline::new("Activity", &values).render(frame, area);
```
