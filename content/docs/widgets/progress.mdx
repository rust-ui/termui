---
title: Progress
description: A labeled percentage progress indicator for a terminal metric.
---

`Progress` renders a percentage with a metric label such as CPU or memory usage.

<RustDemo name="progress" src="/demos/interactive.html?demo=progress-interactive" title="Progress" rows={5} code={`use termui_widgets::progress::Progress;

Progress::new("CPU", 80).render(frame, area);`} />

## Source

<ComponentSource src="crates/termui-registry/src/widgets/src/progress.rs" title="progress.rs" language="rs" collapsible={false} />

## Usage

```rust
use termui_widgets::progress::Progress;

Progress::new("CPU", 80).render(frame, area);
```
