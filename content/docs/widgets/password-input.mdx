---
title: Password Input
description: Display a password value as masking characters.
---

`PasswordInput` masks a value. Your application owns editing and secret storage.

<RustDemo name="password-input" title="Password Input" rows={6} code={`use termui_widgets::password_input::PasswordInput;

PasswordInput::new("Password", value).render(frame, area);`} />

## Source

<ComponentSource src="crates/termui-registry/src/widgets/src/password_input.rs" title="password_input.rs" language="rs" collapsible={false} />

## Usage

```rust
use termui_widgets::password_input::PasswordInput;

PasswordInput::new("Password", value).render(frame, area);
```
