---
title: Tabs
description: A static tab row with a caller-selected tab.
---

`Tabs` renders the current tab and neighboring labels. Your application owns navigation and selection.

<RustDemo name="tabs" src="/demos/interactive.html?demo=tabs-interactive" title="Tabs" rows={5} code={`use termui_widgets::tabs::Tabs;

let labels = ["Overview", "Settings", "Logs"];
let line = Tabs::new(&labels, 0).line();`} />

## Source

<ComponentSource src="crates/termui-registry/src/widgets/src/tabs.rs" title="tabs.rs" language="rs" collapsible={false} />

## Usage

```rust
use termui_widgets::tabs::Tabs;

let labels = ["Overview", "Settings", "Logs"];
let line = Tabs::new(&labels, 0).line();
```
