[workspace]
resolver = "2"
members = [
    "crates/termui-registry/src/widgets",
    "crates/termui-renderer",
    "crates/termui-registry",
]

[workspace.package]
edition = "2024"
license = "MIT"

[profile.release]
codegen-units = 1
lto = true
opt-level = "s"
strip = true
