[package]
name = "termui-demo-key-bar"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
termui-widgets = { path = "../termui-widgets" }
ratzilla = "0.3"
critical-section = { version = "1", features = ["std"] }
